#include "Heap.h"
int main()
{
    HP hp;
    HPInint(&hp);
    HPPush(&hp,10);
    HPPush(&hp,20);
    HPPush(&hp,30);
    HPPush(&hp,22);
    HPPush(&hp,27);
    HPPush(&hp,34);
    HPPush(&hp,15);
    HPPush(&hp,5);
    return 0;
}
